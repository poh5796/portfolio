# Portfolio

Single-page resume site for **Poh Hong Chin**, built with React and Vite in a neo-brutalist visual style.

## Setup

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` — local development server
- `npm run build` — production build
- `npm run preview` — preview the production build
